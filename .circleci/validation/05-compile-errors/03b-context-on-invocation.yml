# 検証5-3b: invocation に context
version: 2.1
jobs:
  deploy:
    docker:
      - image: cimg/base:current
    steps:
      - run: echo deploy
job-groups:
  deploy-group:
    jobs:
      - deploy
workflows:
  w:
    jobs:
      - deploy-group:
          context: org-global
